apiVersion: v1
kind: Service
metadata:
  name: agentic-api
  labels:
    app.kubernetes.io/name: agentic-api
    app.kubernetes.io/component: gateway
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: agentic-api
    app.kubernetes.io/component: gateway
  ports:
    - name: http
      port: 9700
      targetPort: http
      protocol: TCP
