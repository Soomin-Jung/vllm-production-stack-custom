# Example only. Do not commit a populated copy.
apiVersion: v1
kind: Secret
metadata:
  name: agentic-api
type: Opaque
stringData:
  DATABASE_URL: "postgresql://agentic-api:replace-me@postgres.example.invalid:5432/agentic_api?sslmode=verify-full"
  # This credential is sent to the upstream inference service. It is not an
  # inbound password for Agentic API callers. Remove the key if not required.
  OPENAI_API_KEY: "replace-if-required"
