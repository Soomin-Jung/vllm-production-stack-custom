apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: inference
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: agentic-api-file-config
    files:
      - config.toml
